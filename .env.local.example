JQUANTS_API_KEY=       # J-Quants V2 APIキー (https://jpx-jquants.com)
NEXT_PUBLIC_SUPABASE_URL=   # SupabaseプロジェクトURL
NEXT_PUBLIC_SUPABASE_ANON_KEY= # Supabase公開キー（クライアント用）
SUPABASE_URL=          # SupabaseプロジェクトURL（サーバーサイド用）
SUPABASE_SERVICE_KEY=  # Supabaseサービスキー（サーバーのみ）
CRON_SECRET=           # cron認証用文字列（openssl rand -base64 32 で生成）
